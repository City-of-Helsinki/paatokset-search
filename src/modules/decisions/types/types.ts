export type FormErrors = {
  from?: string,
  to?: string
};

export type Fields = {
  top_category_name: string,
  meeting_date: string,
  decision_url: string,
  policymaker: string,
  subject: string
};
